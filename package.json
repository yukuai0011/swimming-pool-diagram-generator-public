{
  "name": "swimlane-gen",
  "version": "0.1.0",
  "description": "Generate vertical swimlane diagrams (SVG) from a Mermaid-like DSL.",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "swimlane-gen": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test"
  },
  "keywords": ["swimlane", "diagram", "svg", "dsl", "mermaid"],
  "license": "MIT",
  "dependencies": {
    "citty": "^0.1.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
